import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ao.arena.oficinatycoon',
  appName: 'Oficina Tycoon',
  webDir: 'dist',
  android: { backgroundColor: '#0d1117' },
};

export default config;
